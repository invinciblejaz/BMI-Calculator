<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">    <!-- State when the EditText is focused (being typed in) -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <stroke
                android:width="2dp"
                android:color="#9C27B0" /> <!-- Use your app's theme color -->
            <corners android:radius="12dp" />
        </shape>
    </item>

    <!-- Default state (not focused) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <stroke
                android:width="1dp"
                android:color="#CCCCCC" /> <!-- Light gray border -->
            <corners android:radius="12dp" />
        </shape>
    </item>

</selector>
